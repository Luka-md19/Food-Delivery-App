#!/usr/bin/env node

// This JavaScript file serves as a direct entry point to the CLI
// It allows running the CLI tool directly without requiring typescript compilation first
require('ts-node/register');
require('./generate-report.ts'); 